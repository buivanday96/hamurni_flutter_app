import 'package:flutter/material.dart';

const colorApp = Colors.teal;
const colorButton = Color(0xff009688);
const colorSplashButton = Colors.green;
const colorButtonInfo = Color(0xff9e9e9e);

/// Login View
const colorEnterPhoneNumber = Color(0xffc4c4c4);

class AppColor {
  static Color primaryColor = Colors.teal;
  static Color primaryLight = Color(0xff52c7b8);
  static Color primaryDark = Color(0xff00675b);
  static Color accentColor = Color(0xff034a51);
  static Color hintColor = Color(0xffc4c4c4);
}
